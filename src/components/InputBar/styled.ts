import styled from 'styled-components';

export const InputBarContainer = styled.div`
    width: 100%;
    height: 54px;
    display: flex;
    flex-wrap: nowrap;
    gap: 8px;
    margin-top: calc(0% - 1.6em);
`;